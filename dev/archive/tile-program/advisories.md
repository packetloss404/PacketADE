# Tile Program — Planner Advisories (code-drift corrections)

Flagged by the planning pass on 2026-07-08. Implementers MUST honor these over any conflicting line-level claim in the design spec.

- Path drift, not a ruling problem: AgentsView.tsx and WorkspaceView.tsx live at src/components/views/, not src/components/agents/. The spec's Phase-3 justification cites a grep for layoutStore/activePaneId 'anywhere under src/components/agents/' — that scope would not have covered AgentsView.tsx itself. The claim still appears true (no activePaneId usage in the agents surface), but the Phase-3 implementer should re-run the grep over both src/components/agents/ and src/components/views/AgentsView.tsx before relying on the 'zero references' premise of the first-commit timing ruling.
- Minor line-number drift in the spec vs current code: the blind cast `parsed as Workspace[]` is at workspaceStore.ts:104 (spec says :103), and other cited line anchors (tauri.ts:1443-1449 whitelist, WorkspaceView.tsx:52/98/240-244, workspaceStore.ts:353/374) should be treated as approximate — Wave-4 merges on main may have shifted them. Implementers should re-anchor by symbol, not line.
- The spec is dated 2026-07-08 and cites verification against the pre-Wave-4 tree in places; the recent Agents-tab Wave-4 merge (virtualization, diff-engine rewrite, sidebar org) touched AgentSidebar machinery that Phase 4's FleetSidebar is built from — the parity checklist should be baselined against the CURRENT AgentSidebar feature set, which may exceed what the spec enumerated (search, pins, archive filter, needs-you counts, project rename, plus any Wave-4 additions like export/sidebar org).
- Ctrl+Shift+V collision: App.tsx:174 already binds Ctrl+Shift+V globally for push-to-talk while AgentsView.tsx:178-198 binds the same combo for transcript view-mode cycling; the P5 hoist of the cycler to App level must explicitly reconcile this double-binding (spec's hoist instruction does not mention the conflict). Not a ruling problem — flagged so P5 peer review tests both behaviors.
- Spec says 'Shift+1 hotkey' but the verified binding is Ctrl+Shift+1 (App.tsx:150 ctrl+shift handler, viewMap '!'->'agents' at :186); P5 must remap the actual chord.
- workspaceStore.focusPaneRequest does not exist in the code — the spec names it as the needs-you focus+flash mechanism as if extant; it is net-new machinery that P4 must build (deep-link retargets in P5 depend on it).
- lib/api-models.ts has no capabilities/supportsApprovals field today — P1 workstream F creates it net-new, as intended, but the merged-catalog registry in P3 depends on its shape being settled in P1.
- agent-sidecar/src/providers/openai-codex.ts header comments (lines 91-100) still document stale '-a on-request' mode mappings; the actual modeToCodexFlags code maps every mode to sandbox + '-a never' (or bypass), confirming the spec's Codex-honesty claim — doc drift only, but worth cleaning in P1 F to avoid future confusion.
- TerminalPane sets layoutStore.activePaneId on onClick (TerminalPane.tsx:99), not pointer-down as the spec loosely phrases; ConversationTile should match the existing convention (or upgrade both consistently) so the Y/N arming gate keys off identical focus semantics.
- A shared sidebar-prefs store (src/stores/agentSidebarPrefsStore.ts) already exists; projectLabels currently lives on agentTaskStore (AgentSidebar.tsx:127) — the P4 move has a ready home, but the P1 eslint store-isolation rule should be written so this prefs store is not accidentally caught by the agentTaskStore/workspaceStore import ban.
- GitDashboard has only ONE live mount today (WorkspaceView.tsx:308); the spec's 'both mount points (WorkspaceView, FlightsView)' claim for the clickable-rows fix is wrong — FlightsView.tsx:457 is a comment reference only. P1-S4 implements rows inside GitDashboard.tsx so any future mount inherits them.
- workspaceStore.focusPaneRequest does not exist anywhere in src (grep-verified); the spec cites it as if existing. Planned as net-new work in P4-S1. layoutStore.activePaneId does exist and is the real mosaic focus mechanism (set by TerminalPane.tsx:99).
- TerminalPane sets activePaneId via onClick, not pointer-down as the spec states; P3-S2 uses pointer-down on ConversationTile per spec intent without touching TerminalPane (protected PTY path) — reviewers must not 'align' TerminalPane.
- Squash-merge conflict recovery in merge_conversation_branch is subtle: git merge --abort does not cleanly apply to squash conflicts, so the ruled 'conflicts leave worktree AND root intact' semantics need an explicit reset/cleanup path proven by byte-level fixture assertions (P2-S1).
- The P3 perf gate (4 streams, 2x2, p95<16ms 'on reference hardware') and P4 mounted-cost gate ('near-zero idle CPU') are not yet executable as written — sprints must define and document a concrete harness/machine or the fallbacks can never trigger.
- openSession's origin:"conversation" workspace marker requires a workspace-level persisted field the spec never schemas (only pane-level kind/conversationId are specified); P1-S2 adds it with the same #[serde(default)] inert pattern — reviewers should gate it like the pane fields.
- App.tsx maps the shifted character '!' to the agents view (~line 186), not a literal Shift+1 check; P5-S1's remap must edit that entry and keyboard-layout variance of '!' is a pre-existing caveat.
- All cited line numbers were re-verified on feat/tile-program at planning time but will drift as sprints land — treat as anchors, not coordinates.
- workspaceStore.focusPaneRequest has zero references anywhere in src today — it is net-new machinery, but the design spec's phrasing ('via workspaceStore.focusPaneRequest', 'the existing mosaic focus mechanism') could be misread as claiming it exists. Only layoutStore.activePaneId exists. The needs-you click, notification deep links, and the Phase-5 redirect all depend on this new field; it needs an explicit home in the Phase 3/4 work breakdown or it will be discovered mid-phase.
- Draft-tile lifecycle is underspecified in the ruled spec: the ordering invariant says the conversation is created in agentTaskStore BEFORE pane insertion, which for the chat-agent draft flow means an unsent draft conversation exists in the store before first send — the spec does not rule whether that draft appears in sessionIndex/the fleet sidebar, or what happens if the workspace closes before first send. features.md asserts 'no orphaned working row may appear' as the behavior to review against, but the mechanism (defer conversation creation to first send vs. filter drafts in the projection) is a small open decision, not a settled ruling.
- The 'worktree pending' chip's surface area is loosely ruled: the spec places it on 'the session' (q5) and Phase 4 mentions it in archive context, but does not enumerate exactly which surfaces show it (tile header, sidebar row, both). features.md specs both as the reviewable behavior; if the build shows it in only one place a reviewer needs to know which is authoritative.
- capabilities.supportsApprovals does not exist yet in lib/api-models.ts (confirmed by grep) — expected, since it is Phase 1 Workstream F, but reviewers of Phase 3's picker should be aware the Codex posture filtering depends on that Phase 1 catalog field landing first.